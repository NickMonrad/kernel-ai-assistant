# AGENTS.md

Project agent context lives in `.omp/AGENTS.md` — that file is the single source of truth and is loaded automatically by OMP. Read it before making changes.

For on-demand reference: `.docs/agents/`
For role-specific config: `.opencode/agents/<role>.md`
